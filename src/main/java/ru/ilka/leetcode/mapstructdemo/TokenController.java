package ru.ilka.leetcode.mapstructdemo;

public class TokenController {
// real controller

/*
package com.touchlane.gaimin.security;

import io.swagger.annotations.ApiParam;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequiredArgsConstructor
public class TokenController {
    private final JwtTokenProvider jwtTokenProvider;

    @GetMapping("/tokens/device/idIntoToken")
    public String deviceIdIntoToken(@ApiParam(example = "169") @RequestParam("id") Integer id) {
        return jwtTokenProvider.generateDeviceToken(id);
    }

    @GetMapping("/tokens/device/tokenIntoId")
    public int deviceTokenIntoId(@ApiParam(example = "token") @RequestParam("token") String token) {
        return jwtTokenProvider.validateAndGetDeviceIdFromToken(token);
    }
}
*/

//
//
}
